[pytest]
# The skill's own tests live inside the skill directory itself
# (skills/wiki-interest-trends/tests/), per the Agent Skills spec's
# self-contained-directory requirement. This lets `pytest` still work
# from the repo root, as documented, without moving the tests back out.
testpaths = skills/wiki-interest-trends/tests
