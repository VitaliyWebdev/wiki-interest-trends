# Runtime + dev dependencies, pinned. Individual scripts under scripts/ also
# declare their own PEP 723 inline metadata for `uv run`; this file is the
# fallback for running everything (including tests) via plain pip/venv.
requests==2.34.2
pytest==9.1.1
matplotlib==3.11.2
reportlab==5.0.1
pypdf==6.19.0  # test-only: used to assert report.pdf is exactly one page
skills-ref==0.1.1  # test-only: the spec's own reference validator, see test_skill_md.py
